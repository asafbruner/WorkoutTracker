# Supabase Configuration (Optional - for cloud sync)
# Get these from your Supabase project settings: https://app.supabase.com
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Password Hash (Optional - for custom password)
# Generate using: console.log(await crypto.subtle.digest('SHA-256', new TextEncoder().encode('your-password')))
# Or use the built-in password change feature in the app
# VITE_PASSWORD_HASH=your-sha256-hash-here

# Default password is "asaf2024" if not set
